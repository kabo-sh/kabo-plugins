interface:
  display_name: "Kabo Revoke"
  short_description: "Revoke Kabo's access on every device, then log this machine out"
  default_prompt: "Use $kabo-revoke to revoke Kabo's access on all my devices."
policy:
  allow_implicit_invocation: false
