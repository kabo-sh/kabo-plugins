interface:
  display_name: "Kabo Analyze"
  short_description: "Analyze a channel, niche or video through verified Kabo skills"
  default_prompt: "Use $analyze to research this creator question with a verified Kabo skill."
dependencies:
  tools:
    - type: "mcp"
      value: "kabo"
      description: "Kabo registry, telemetry, and creator research tools"
      transport: "streamable_http"
      url: "https://kabo.sh/mcp"
policy:
  allow_implicit_invocation: true
